<%= form_with(url: :flights, local: true, html: {id: 'logtenpro-form', class: 'mt-2'}) do |form| %>
  <%= form.file_field :logtenpro, class: 'btn btn-secondary' %>
  <%= form.submit class:'btn btn-primary', id: 'upload-logbook-btn', disabled: true %>
<% end %>
