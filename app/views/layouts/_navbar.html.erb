<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to 'ARELPLANE', root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <%= link_to 'MANAGE FLIGHTS', flights_path, class: 'nav-link' %>
        </li>
        <li class="nav-item active">
          <%= link_to 'LOG OUT', destroy_user_session_path, method: :delete, class: 'nav-link' %>
        </li>
      </ul>
      <ul class="navbar-nav">
        <% if @user == current_user %>
          <li class="nav-item active">
            <button href="#" data-toggle='modal' data-target='#logbookUploadModal' class='btn btn-primary'>Upload Logbook</button>
          </li>
        <% end %>
      </ul>
    </div>
</nav>
