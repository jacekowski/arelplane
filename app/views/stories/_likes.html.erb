<% if user_signed_in? && current_user.likes?(post) %>
  <%= link_to "Unlike", story_like_path(post), method: :delete, remote: true %>
<% else %>
  <%= link_to "Like", story_like_path(post), method: :post, remote: true %>
<% end %>

<%= post.likes.count %>

<% post.likes.each do |like| %>
  <%= image_tag like.user.avatar_url, width: 20 %>
<% end %>
